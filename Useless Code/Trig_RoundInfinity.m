function Trig_RoundInfinity()

x = sym('x'); % for now this is a theorhetical distance
zstep = -1; 
zraise = sym('zr'); % again, theorhetical. I don't care how high it is.
for y = 1:1:3 % Three inches of forward movement at a time?
    L = sym('L'); % The horizontal components of leg lengths
    t2 = asind(y/L);


















end